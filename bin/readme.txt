README
——————
If running in Eclipse IDE, keep the input.txt file in src folder and keep fileName = "src/input.txt".

If not running in Eclipse IDE, give the path to read the input file as fileName = "<path/to/file>".

N is the number of rows and columns in the puzzle.